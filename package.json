{
  "name": "ethicalalt",
  "description": "EthicalAlt — React + Express investigative shopping lens (photo, tap, sourced corporate record + local alternatives)",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w ethicalalt-server\" \"npm run dev -w ethicalalt-client\"",
    "build": "npm run build -w ethicalalt-client",
    "start": "npm run start -w ethicalalt-server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
